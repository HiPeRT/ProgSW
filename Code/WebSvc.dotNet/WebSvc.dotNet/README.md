# Simple Web Service in dotnet

This is an example of a dotnet WebApi.

You can run some simple tests (see the <i>Tests</i> project) or run the full server.

 You can do it by setting it as "startup project" in the context menu of Visual Studio, and then pressing F5, or by running the following CLI command in the project folder.
 
 ```
 $ dotnet run
 ```